import { combineReducers } from "redux";
import contact from "./contact";

export const rootReducer = combineReducers({
  contact
})

// In this file, we are creating a root reducer using the combineReducers function from Redux.